[package]
name = "crayon-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Platform-independent product identity and domain foundations"
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
serde_json = "1"
