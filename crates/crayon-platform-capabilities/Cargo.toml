[package]
name = "crayon-platform-capabilities"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Read-only platform adapter capability model (PLT-02): wire schema, validation and consistency folding"
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
